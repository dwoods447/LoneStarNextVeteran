<%= form_for(certifier) do |f| %>
  <% if certifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(certifier.errors.count, "error") %> prohibited this certifier from being saved:</h2>

      <ul>
      <% certifier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :certifier_id %>
    <%= f.number_field :certifier_id %>
  </div>

  <div class="field">
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>


  <div class="field">
    <%= f.label :benefit_assigned %>
    <%= f.text_field :benefit_assigned %>
  </div>

    <div class="field">
      <%= f.label 'Upload a Photo' %>
      <%= f.file_field :avatar %>
    </div>


    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
